```markdown
# HW01 - My ChatGPT Clone

本專案為基於 **FastAPI** 與 **HTML/JS** 實作之網頁端 ChatGPT 應用程式，已串接最新版 **Google Gemini API**，並完整實作講義要求之五大核心功能。

##  作者資訊
* **系級：** 交大資工系 112 級
* **學號：** 112550055
* **姓名：** 吳岡達

---

##  一、環境建置與啟動方法

### 1. 配置金鑰
* 在 `backend` 資料夾下建立 **`.env`** 檔案。
* 寫入 Google Gemini 的 API Key (可至 Google AI Studio 免費申請)：
  ```text
  GEMINI_API_KEY=你的金鑰字串

```

### 2. 安裝依賴套件

* 開啟終端機 (建議使用 Anaconda Prompt)。
* 啟動虛擬環境 (請根據自身資料夾名稱修改)：
```bash
conda activate hw01_env

```


* 進入 `backend` 目錄。
* 執行以下指令安裝所需套件：
```bash
pip install fastapi uvicorn python-dotenv google-genai

```



### 3. 啟動伺服器

* 執行以下指令啟動 FastAPI：
```bash
uvicorn main:app --reload

```


*  **注意：** 測試期間 **請勿關閉** 此終端機視窗。

### 4. 開啟網頁

* 直接雙擊開啟 **`frontend/index.html`** 檔案，即可開始使用。

---

##  二、五大核心功能測試指南

### 1. LLM 模型切換測試 與 Streaming 串流輸出測試

* **操作：** 於左側下拉選單切換模型。
* **輸入提示詞：** > 「請給我一篇 1000 字的繁體中文文章，介紹人工智慧的歷史。」
* **驗證：** 觀察畫面是否呈現 **逐字輸出的打字機效果**，且模型能正常切換並回覆。

### 2. System Prompt 測試

* **操作：** 於左側 System Prompt 欄位輸入以下人設：
> 「你現在是一位脾氣暴躁的資深程式設計師，對於使用者的問題極度不耐煩，且每句話都必須以『菜鳥，聽好了！』作為開頭。」


* **驗證：** 發送任意問題，驗證模型回覆是否 **嚴格遵守上述語氣設定**。

### 3. Temperature 測試

* **操作：** 測試前請先 **重新整理網頁** 清空歷史紀錄。接著輸入以下提示詞：
> 「請幫我發明 3 個科幻小說裡的外星人名字，並簡述特徵。」


* **驗證：** 將 Temperature 調整至不同數值測試，觀察 **低溫時的穩定性** 與 **高溫時的隨機創意發散**。

### 4. 交談短期記憶測試

* **操作 (第一回合)：** 傳送以下背景資訊：
> 「我最喜歡的水果是香蕉，而且我的寵物是一隻叫做『雷神』的烏龜。」


* **操作 (第二回合)：** 待模型回覆後，緊接著詢問：
> 「根據我們剛剛的對話，請問我養了什麼寵物？牠叫什麼名字？那我喜歡吃什麼？」


* **驗證：** 系統能 **精準回覆上一回合的記憶內容**。

---

##  三、資安防護與繳交規範

### 1. 程式碼上傳至 GitHub

* **注意 API KEY 的保護：** 絕對 **不可** 將 API 金鑰直接寫死在程式碼中。
* **注意環境變數的保護：** 需建立 **`.gitignore`** 檔案，確保 `.env` 檔案被完全隱藏，**不會** 被推送至 GitHub 遠端儲存庫。

### 2. E3P 平台繳交項目

* **繳交 GitHub URL**。
* **繳交 Demo 錄影檔 (3分鐘)：** 影片內需 **完整展示上述五大功能** 之實際運作。

```

```